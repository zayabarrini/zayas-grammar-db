[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.104.0"
uvicorn = ">=0.24.0"
psycopg2-binary = ">=2.9.0"
sqlalchemy = ">=2.0.0"
alembic = ">=1.12.0"
python-multipart = ">=0.0.6"
pydantic = ">=2.0.0"
python-dotenv = ">=1.0.0"
pytest = ">=7.4.0"
requests = "*"

[dev-packages]
black = ">=23.0.0"
flake8 = ">=6.0.0"

[requires]
python_version = "3.12"
